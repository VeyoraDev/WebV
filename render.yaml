services:
  - type: web
    name: vstore
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: SESSION_SECRET
        generateValue: true
    disk:
      name: vstore-data
      mountPath: /opt/render/project/src/data
      sizeGB: 1
